# Default runtime install for the production codepath.
# For contributor tooling, use `requirements/dev.txt`.
# For notebooks and exploratory work, use `requirements/research.txt`.
-r requirements/app.txt
